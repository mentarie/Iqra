<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="proyek.android.iqra">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_iqra"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_iqra"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Pengantar1Activity"></activity>
        <activity android:name=".ToolbarTesBaca" />
        <activity android:name=".ToolbarLatihan" />
        <activity android:name=".ToolbarPengantar" />
        <activity android:name=".ToolbarHome" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>